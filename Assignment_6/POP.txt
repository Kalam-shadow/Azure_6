POP


sudo apt install virtualbox-guest-utils
sudo mount | grep foldername in ubuntu 

sudo dmesg | more
more /var/log/syslog
tail "
tail -100 /var/log/syslog | more

cat /proc/cpuinfo
lscpu

sudo sysctl -a | grep hw



top

vmstat/vmstat 2 - seconds

--------------------------------------------------------

last 

id <username>

w

finger

getent passwd <username>

getent group <grpname>

who


sudo bash # root user access

sudo usermod -aG groupname username

sudo usermod -i newusername oldusername

sudo chage -l test1 # lists passwords expiry info

sudo chage -M 30 test1 # sets he pass to expire in 30 days

deluser --remove-home test1

sudo vi/etc/sudovrs




groupssss

addgroup testgroup

getent group <groupname>

chgrp //;

sudo delgroup <groupname>


stat <file path>

dumpe2fs -h /dev/sda3 | grep superblock

[
inode count - max files
block count - maximum space  can be allotted over all files 

free block - space which is free
free inode - .....
]




which <something>  - shows pathname of <something>

file path 

readelf -- reads executable scripts

find ~/c-type f -exec du -h {} \;

find .size + 1M

find .-type d

find .-name ".txt"

tail -f <filename>


ln pathfile pathfile  		- creates a copy
ln -s pathfile pahfile 		- creates a pointer or symbolic links

watch -n 10 'df -h .'
inotifywait -mr  foldername	- shows actions in that folder



pstree 

ps

ps -x --forest


sudo MySQL -u root -p		- to access the mysql


nslookup .com    	- ipaddress

sudo iptables -A OUTPUT -d <ip> -j DROP 	- blocks or stops packet
sudo iptables -D OUTPUT -d <ip> -j DROP 	- unblocks

sudo iptraf
sudo iftop




-------------------------------------------------------------------------

shell scripting

for i in { 1 .. 5 }

